require "spec_helper"

RSpec.describe Chewy::Diff do
  it "has a version number" do
    expect(Chewy::Diff::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
